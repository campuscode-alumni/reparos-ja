<%= form_for ([@service_order, @user_review]) do |f| %>
  <%= f.label :rating %>
  <%= f.select :rating, options_for_select((0..5).to_a) %>
  <%= f.label :comment %>
  <%= f.text_area :comment %>
  <%= f.submit t(:submit_review) %>
<% end %>